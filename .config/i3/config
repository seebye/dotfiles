# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# super key
set $mod Mod4
# alt key
set $alt Mod1

# data files
set $filemode /tmp/i3mode

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:Source Code Pro, Awesome 12
#font pango:DejaVu Sans Mono 11
#font pango:monospace 10.5
font pango:Ubunut Mono derivative Powerline 10

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
# useless - event will never reach i3 
floating_modifier $mod


popup_during_fullscreen smart

# jump back to the last workspace 
# current ws = 1
# mod+4
# current ws = 4
# mod+4
# current ws = 1
# done by scripts - otherwise wrong events - workspace_auto_back_and_forth yes
# go to the last workspace via mod+z
#bindsym $mod+z workspace back_and_forth
# move wnd to last workspace + go back to the last ws
#bindsym $mod+Shift+z move container to workspace back_and_forth; workspace back_and_forth


set $w1 1:www
set $w2 2:ide
set $w3 3
set $w4 4
set $w5 5
set $w6 6
set $w7 7
set $w8 8
set $w9 9
set $w0 0:Tails

# move to .c/w/k
bindsym $mod+slash scratchpad show
bindsym $mod+Shift+n exec $HOME/i3/list_workspaces.sh
bindsym $mod+n exec scripts/additional_ws.sh


# disable auto focus via mouse - needed for resize mode
focus_follows_mouse no

# resize window (you can also use the mouse for that)
mode "resize" {
#	focus_follows_mouse no
		# These bindings trigger as soon as you enter the resize mode

		# Pressing left will shrink the window’s width.
		# Pressing right will grow the window’s width.
		# Pressing up will shrink the window’s height.
		# Pressing down will grow the window’s height.
		bindsym h resize shrink width 5 px or 5 ppt
		bindsym t resize grow height 5 px or 5 ppt
		bindsym n resize shrink height 5 px or 5 ppt
		bindsym s resize grow width 5 px or 5 ppt

		# same bindings, but for the arrow keys
		bindsym Left resize shrink width 5 px or 5 ppt
		bindsym Down resize grow height 5 px or 5 ppt
		bindsym Up resize shrink height 5 px or 5 ppt
		bindsym Right resize grow width 5 px or 5 ppt

		# back to normal: Enter or Escape
		bindsym Return mode "default" ; exec echo "" > $filemode ; exec pkill -f resize_window_mouse.py
		bindsym Escape mode "default" ; exec echo "" > $filemode ; exec pkill -f resize_window_mouse.py
	bindsym $mod+r mode "default" ; exec echo "" > $filemode ; exec pkill -f resize_window_mouse.py

	bindsym $mod+m focus floating; mode "move" ; exec echo move > $filemode ; exec pkill -f resize_window_mouse.py ; exec .bin/move_window_mouse.py
}

bindsym $mod+r mode "resize" ; exec echo resize > $filemode ; exec .bin/resize_window_mouse.py
# ; exec python script which execs i3-msgs to resize the windows by position changes of the mouse 

mode "move" {
#	focus_follows_mouse no

	bindsym Up move up 20px
	bindsym Left move left 20px
	bindsym Down move down 20px
	bindsym Right move right 20px

	# back to normal: Enter or Escape
	bindsym Return mode "default" ; exec echo "" > $filemode ; exec pkill -f .bin/move_window_mouse.py
	bindsym Escape mode "default" ; exec echo "" > $filemode ; exec pkill -f .bin/move_window_mouse.py
	bindsym $mod+m mode "default" ; exec echo "" > $filemode ; exec pkill -f .bin/move_window_mouse.py

	bindsym $mod+r mode "resize" ; exec echo resize > $filemode ; exec pkill -f .bin/move_window_mouse.py ; exec .bin/resize_window_mouse.py
}
bindsym $mod+m focus floating; floating enable; mode "move" ; exec echo move > $filemode ; exec .bin/move_window_mouse.py
# ; exec python script which execs i3-msgs to move the windows by position changes of the mouse 



# Gaps
#smart_gaps on
gaps outer 0 # 10
gaps inner 0
workspace $w1 gaps outer 0
for_window [class="^.*"] border pixel 0
#for_window [class="URxvt"] border pixel 0
new_window none
#new_float pixel 12
for_window [class="XTerm"] border pixel 2
# floating 12pixel but how..
hide_edge_borders both


#set $transparent #00000050
set $transparent #00000000

set $black       #1c1815
set $darkblack   #2a2720
# set $red         #bd5d4c
#set $red         #fb4757
set $red         #D15F46
set $darkred     #7a3225
set $green       #4d6733
set $darkgreen   #4d6633
set $yellow      #917033
set $darkyellow  #7a5b20
set $blue        #708294
set $darkblue    #3c5165
set $magenta     #91313b
set $darkmagenta #7a232c
set $cyan        #319170
set $darkcyan    #1c7a59
set $white       #ffffff
#c8c6c3
set $txtbar      #f0f0f0
#set $txtbar      #f0f0f0
set $darkwhite   #c8b49d
bar {
	position top
	status_command i3blocks
	i3bar_command i3bar -t -w "$HOME/i3/switch_workspace_name.sh"
	#font pango:Ubuntu Mono, FontAwesome 11
	font pango:Ubuntu Mono derivative Powerline, Elusive-Icons 8, FontAwesome 10
	separator_symbol "|"
	#tray_output eDP1
	tray_padding 0
	#height 28
	height 20 #windoof taskbar height = 40px - 20px=titlebar height i3 !!!!! title bar height changes with the font family/text size
	#strip_workspace_numbers yes

	colors {
		statusline		 $txtbar #$black #$white
		background		 $transparent
		separator		 $darkwhite

		#		   BORDER	BACKGROUND   TEXT
		focused_workspace  $transparent $transparent $tb_fg
		inactive_workspace $transparent $transparent $txtbar
		active_workspace   $transparent $transparent $txtbar
		urgent_workspace   $darkblack   $darkblack   $red
		binding_mode	   $transparent $transparent $yellow
	}
}
#set $child_border	#BF7171
#set $child_border	#896266
#set $child_border	#6D4B4F	
#set $child_border	#C7928A
set $child_border	#1E1C1C
#set $tb_bg		#7B0A13
set $tb_bg		#1e1c1c
#set $tb_bgf		#D15F46
set $tb_bgf		#D7875F
#set $tb_bgf		#E64B29
#set $tb_bgf		#FE7457
#set $tb_bgf		#940C17
#set $tb_bg		#333333
#set $tb_fg		#a0a0a0
#set $tb_fgf		#ffffff
set $tb_fg		#fe7457
set $tb_fgf		#050505
#set $tb_fgf		#1e1c1c
# window colors
#			border		background		text		indicato
client.focused		$tb_bgf		$tb_bgf			$tb_fgf		#151515		$child_border
client.unfocused	$tb_bg		$tb_bg			$tb_fg		#040405		$child_border
client.focused_inactive $tb_bg		$tb_bg			$tb_fg		#040405		$child_border
client.urgent		$tb_bg		$tb_bg			$tb_fg		#151515		$child_border

# window title
for_window [class=".*"] title_format <span size="smaller"> %title</span>


# keyevents not received by weiche
# Screen brightness controls
bindsym XF86MonBrightnessUp exec $HOME/i3/brightnessup.sh # increase screen brightness
bindsym XF86MonBrightnessDown exec $HOME/i3/brightnessdown.sh # decrease screen brightness
# default: xbacklight -set 33.938100

#
#
# Window rules
#
#

# window title
#for_window [class=".*"] title_format <span size="smaller"> %title</span>
# window title sandbox
#for_window [class="Xephyr"] title_format <span size="smaller"><span color="#47FB6E"><b>Sandbox </b></span>%title</span>

# fix floating terminal - enable floating mode by default + move it
for_window [instance="altyo"] floating enable , exec i3-msg move down 38px

# fix launcher position
# 1600/2-600/2  900/2 - 50
for_window [class="albert"] sticky enable , move position 500px 250px

# enable floating window for pidgin by default
for_window [class="Pidgin"] floating enable

# browser pop-ups
for_window [window_role="pop-up"] floating enable

#exec autokey-gtk
for_window [instance="keepass"] floating enable , move scratchpad

# firejail sandbox windows shouldn't use the floating mode
for_window [class="Xephyr"] floating disable

# calendar popup
for_window [class="XTerm" title="Calendar"] floating enable, resize set 216 173, move window to position 1376 25

# cpu profile popup
for_window [class="XTerm" title="CPU Profile"] floating enable, resize set 116 110, move window to position 1313 25

# audio output popup
for_window [class="XTerm" title="Audio Output"] floating enable, resize set 416 10, move window to position 1113 25

# enable floating mode for xterm menus
for_window [class="XTerm" title="XTerm Menu Script"] floating enable, resize set 1 1, move window to position 1600 900


# executed after boot
exec $HOME/i3/event/boot.sh
# executed after restart (i3)
exec_always $HOME/i3/event/i3_restart.sh


# Unclutter to hide cursor
# exec unclutter -root -jitter 5

# switch to workspace 1
#exec i3-msg workspace $w1
